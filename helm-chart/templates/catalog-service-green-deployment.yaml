apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: catalog-service-green
  labels:
    app: {{ .Chart.Name }}-selector-catalog-service
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.catalogServiceGreen.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-catalog-service
        deployment: catalog-service-green
    spec:
      containers:
      - name: {{ .Values.catalogServiceGreen.container.name }}
        image: "{{ .Values.catalogServiceGreen.image.repository }}:{{ .Values.catalogServiceGreen.image.tag }}"
        imagePullPolicy: {{ .Values.catalogServiceGreen.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.catalogServiceGreen.image.repository }}:{{ .Values.catalogServiceGreen.image.tag }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.catalogServiceGreen.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}

