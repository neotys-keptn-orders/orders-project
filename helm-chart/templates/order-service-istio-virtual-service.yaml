apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: orders-project-dev-order-service
spec:
  hosts:
  - "order-service.orders-project-dev.35.246.100.192.xip.io"
  gateways:
  - orders-project-dev-gateway
  http:
    - route:
      - destination:
          host: order-service.orders-project-dev.svc.cluster.local
