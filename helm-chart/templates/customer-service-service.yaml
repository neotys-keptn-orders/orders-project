apiVersion: v1
kind: Service
metadata:
  name: customer-service
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  type: {{ .Values.customerService.service.type }}
  ports:
  - port: {{ .Values.customerService.service.externalPort }}
    targetPort: {{ .Values.customerService.service.internalPort }}
    protocol: TCP
    name: {{ .Values.customerService.service.name }}
  selector:
    app: {{ .Chart.Name }}-selector-customer-service